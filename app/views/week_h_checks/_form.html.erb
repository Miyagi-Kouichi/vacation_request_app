<%= form_with(model: week_h_check, local: true) do |form| %>
  <% if week_h_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(week_h_check.errors.count, "error") %> prohibited this week_h_check from being saved:</h2>

      <ul>
        <% week_h_check.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
