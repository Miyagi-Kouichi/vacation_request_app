<%# <%= render "shared/header"%>
<%= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f| %>

  
  <%= render "users/shared/error_messages", resource: resource %>
  <div class='form-wrap'>

    <div class="header">
      <div class="form-header-text">
        <h1>新規登録画面</h1>
      </div>
    </div>

    <div class="form-group">
      <div class='form-text-wrap'>
        <label class="form-text">名前</label>
        <%= f.text_area :user_name, class:"input-default", id:"user_name", maxlength:"20" %>
      </div>

      <div class='form-text-wrap'>
        <label class="form-text">メールアドレス</label>
        <%= f.email_field :email, class:"input-default", id:"email", placeholder:"会社用のメールアドレス", autofocus: true %>
      </div>
      

      <div class='form-text-wrap'>
        <label class="form-text">パスワード</label>
        <%= f.password_field :password, class:"input-default", id:"password", placeholder:"6文字以上の半角英数字" %>
      </div>
      

      <div class='form-text-wrap'>
        <label class="form-text">役職</label>
        <%= f.select :position, ['一般社員', 'リーダー', '主任', '部長', '役員', 'システム管理者'], {include_blank: '---'} %>
      </div>
    
      <div class='register-btn'>
        <%= f.submit "Sign up" ,class:"register-red-btn" %>
      </div>
    </div>
  </div>
<% end %>