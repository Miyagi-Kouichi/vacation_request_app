<%= render partial: "header", locals: { user: @user } %>
  
<div class="r-holiday">
     <h2> <%= @user.user_name %>さん</h2>
</div>
<div class="edit-form">
     <h1>編集ページ</h1>
     <%= form_with model: @remaining_holiday, local: true do |f| %>
          <%= render :partial => 'form', :locals => {:f => f, :edit_flag => true} %>
     <% end %>
</div>